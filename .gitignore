# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.pytest_cache/
.ruff_cache/

# Virtual environment (uv)
.venv/

# Raw scan data — never committed. The dataset is released only at thesis
# submission (PLAN.md K-10); the SQLite file is created at runtime.
data/

# Local design reference screenshots (large PNGs); the design itself is
# recorded in docs/DESIGN-SYSTEM.md + karne/web/static/tokens.css.
docs/design-refs/

# OS / editor noise
.DS_Store
Thumbs.db
.idea/
.vscode/

# Claude Code local (per-machine) settings — the shared skill under
# .claude/skills/ IS committed; only the local settings file is ignored.
.claude/settings.local.json
