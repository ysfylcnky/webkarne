"""Analysis layer: turns raw scan results into scores and findings.

This layer is deliberately separate from collection (see docs/PLAN.md, K-02) and
reads its rules from ``config/scoring.toml`` (K-03). Out of scope for Sprint 0.
"""
